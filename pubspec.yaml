name: ipp
description: A starting point for Dart libraries or applications.
version: 1.0.0
homepage: https://github.com/almpazel/ipp

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  http: any

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
